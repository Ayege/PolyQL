name: 'unary negation'
description: 'A leading sign over a range aggregation.'
source_dsl: promql
target_dsl: logql
input: '-rate(x[5m])'
expected_output: '-rate({__name__="x"}[5m])'
expected_fidelity_score: 0.8571
expected_flags:
  - path: 'Query'
    flag: PARTIAL
  - path: 'Query.Pipeline[0].UnaryOpStage'
    flag: FULL
notes: 'Both languages write a unary sign the same way.'
